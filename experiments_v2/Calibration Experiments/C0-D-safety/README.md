# C0-D — Safety definition and planning margin

Owner scope: `d_hard`, `d_plan` baseline and `s_max`. The meaning of `s`, its
hard-anchored linear mapping structure and allocator objective remain frozen.
