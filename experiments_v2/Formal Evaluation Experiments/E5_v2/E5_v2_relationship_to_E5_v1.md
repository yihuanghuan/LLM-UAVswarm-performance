# Relationship between E5-v1 and E5-v2

E5-v1 remains immutable historical formal evidence. Its authoritative registry is `E5-exact-end-to-end-v1`, baseline tag `paper-final-sim-v3`, configuration `paper-current-v11-c0-f-frozen`.

The old E5-v1 REL-QUAL result remains exactly as reported:

- 5/5 attempts reached the frozen resolver and were scientific-complete;
- 0/5 achieved mission success;
- the registered `spacious sphere` physical realization conflicted with frozen workspace/safety geometry;
- deterministic rejection is retained as valid boundary-respecting, fail-closed evidence.

E5-v2 does not fix, alter, rerun, replace, or reinterpret that command. E5-v2A is a separately preregistered positive-integration study. Its new commands enter the candidate registry only after a prospective deterministic analysis establishes a non-empty physical realization under the same frozen policy. It evaluates feasible semantic realization; the old REL-QUAL evaluates correct rejection of an infeasible request.

E5-v1 MIXED-HIGH also remains unchanged: 0/5 scientific-complete. E5-v2 introduces no redesigned “MIXED-HIGH-v2” and makes no claim that this limitation was erased.

The old and new registries differ. Their success proportions must never be pooled, and reporting must not say “E5 success improved from 56% to X%.” E5-v2 is integration evidence only and supplies no new causal evidence for C1, C2, or C3.
