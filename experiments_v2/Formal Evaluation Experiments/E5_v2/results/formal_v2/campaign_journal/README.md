# E5-v2 formal campaign journal

The authoritative cursor is the validated prefix of immutable per-position JSON
records in this directory. There is no mutable cursor file. The zero-record
initial state resolves to registered slot 1, `E5V2-B-S2-N12-R1` (seed 5202036).
