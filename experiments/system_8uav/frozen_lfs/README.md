# Experiment 10 v3 frozen LFS

These semantic-validated LFS payloads are used only by execution-only replay.

- `task_b_sequential.json`: representative v2 variant from
  `exp10-formal-v2-gated-20260803/task_b_sequential/attempt_0018`.
- `task_e_mixed.json`: representative v2 variant from
  `exp10-formal-v2-gated-20260803/task_e_mixed/attempt_0034`.

The runner records the canonical SHA-256 in every replay manifest. End-to-end
formal trials continue to invoke the frozen MiniMax parser configuration.
